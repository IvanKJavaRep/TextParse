section это просто отсек, который содержит какие-то еще блоки и не надо создавать такой объект в domain.

как найти literal в тексте?

есть ли передача по ссылке в джаве?

((ListItemImpl)((ListImpl)(n).getBlocks().get(0)).getItems().get(0)).getText();